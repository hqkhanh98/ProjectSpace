local enemy = {}
  local enemy.create( enemy, options )
    local options = options or {}
    local x = options.x or 160
    local y = options.y or 200
    local w = options.w or 50
    local h = options.h or 80
    local name = options.name or "giant"
  end
return enemy
